'use strict';

const array1 = ['hello', 'Iam ', 'fadi', 'iam 26'];
const array2 = ['iam', 'a student', 'in','hack', 'your', 'future', 'the best coding school'];

console.log('the lenght of array1 is ' + array1.length);
console.log('the lenght of array2 is ' + array2.length);

if (array1.length == array2.length){
    console.log('They are the same')
}
else{
    console.log('diffirent sizes')
}